<div class="container">
  <div class="row">
    <div class="col-xs-8 story-new">
      <h2>New Layout</h2>
      <%= simple_form_for @layout do |f| %>
        <%= f.input :name %>
        <%= f.button :submit, class: 'btn-default' %>
      <% end %>
    </div>
  </div>
</div>
