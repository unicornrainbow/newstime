<div class="container">
  <div class="row">
    <div class="col-xs-8 story-new">
      <h2>New Photo</h2>
      <%= simple_form_for @photo do |f| %>
        <%= f.input :name %>
        <%= f.input :attachment %>
        <%= f.button :submit, class: 'btn-default' %>
      <% end %>
    </div>
  </div>
</div>
