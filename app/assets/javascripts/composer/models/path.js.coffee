#= require ./content_item

# class @Dreamtool.Path extends Newstime.ContentItem
#
#   setStartingPoint: (x, y) ->
#     @set('points', [[x, y]])
#
#   addPoint: (x, y) ->
#     @get('points').push([x, y])
