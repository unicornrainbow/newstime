<%= simple_form_for [:admin, user] do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :organization_id, collection: Organization.all, as: :select %>
  <%= f.button :submit, class: 'btn-default' %>
<% end %>
