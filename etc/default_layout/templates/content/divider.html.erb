<hr class="divider <%= orientation %>"
  <%= "divider-control data-content-item-id=#{id}" if composing? %>
  style="<%= "top: #{top}px; left: #{left}px; width: #{width}px; height: #{height}px; border-width: #{border_width}; margin: #{margin}; position: absolute;" %>">
