<link href='http://fonts.googleapis.com/css?family=Open+Sans:400italic,400,700,300' rel='stylesheet' type='text/css'>
<style>
  body {
    font-family: 'Open Sans';
  }
  .entry {
    padding: 2pt;
    margin: 1em auto;
    width: 600pt;
  }
  .entry img {
    max-width: 500px;
    border-radius: 4px;
  }
  textarea {
    width: 589pt;
    height: 400px; /* Set up auto height */
    border-width: 1px;
    box-shadow: none !important; /* disable bootstrap form glow */
    border-radius: 0 !important;
  }
</style>
<div class="entry">
  <%= form_tag "/entries/#{@entry.path}" do %>
    <h2><%= @entry.formatted_date_time %></h2>
    <textarea name=""><%= @entry.markdown %></textarea>
    <br>
    <a href="javascript:history.back();">Cancel</a>
    <input class="btn btn-primary pull-right" type="submit" value="Save Changes"></input>
  <% end %>
</div>
