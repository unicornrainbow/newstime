<div class="container">
  <div class="btn-group pull-right">
    <%= link_to 'New', new_story_path, class: 'btn btn-link' %>
  </div>
  <hr>

  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Stories</th>
      </tr>
    </thead>
    <% @stories.each do |story| %>
      <tr>
        <td><%= link_to story.name, story %></td>
      <tr>
    <% end %>
  </table>

</div>
